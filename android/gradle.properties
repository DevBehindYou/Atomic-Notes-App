org.gradle.jvmargs=-Xmx4G
android.useAndroidX=true
# Jetifier (support-library -> AndroidX rewriting) is no longer needed: every
# plugin this app depends on ships AndroidX. Leaving it on just slows builds.
android.enableJetifier=false
